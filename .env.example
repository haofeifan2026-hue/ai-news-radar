# Optional. Used only for AI summaries and topic tagging.
OPENAI_API_KEY=

# Optional. A GitHub token raises API rate limits; public collection works without it.
GITHUB_TOKEN=

# Optional. Official X API bearer token. Leave empty when using x.rss_urls.
X_BEARER_TOKEN=
